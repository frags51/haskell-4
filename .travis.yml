{
  "dist": "trusty",
  "sudo": "require",
  "compiler": "ghc",
  "before_install": [
    
  ],
  "script": [
    "sudo apt-get install haskell-platform"
    "ghc --version",
    "./make.sh"
  ],
  "group": "stable",
  "env": "MATRIX_EVAL=\"CC=ghc\"",
  "os": "linux"
}
